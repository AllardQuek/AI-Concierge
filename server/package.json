{
  "name": "mulisa-server",
  "version": "1.0.0",
  "description": "Mulisa AI voice platform - WebRTC signaling server",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production node index.js",
    "dev": "NODE_ENV=development nodemon index.js",
    "build": "echo 'No build step needed for Node.js server'",
    "test": "NODE_ENV=test node index.js",
    "validate": "node -e \"require('./config/env').validateConfig()\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.0.1",
    "express": "^4.18.2",
    "microsoft-cognitiveservices-speech-sdk": "^1.37.0",
    "socket.io": "^4.7.5",
    "uuid": "^9.0.1",
    "ws": "^8.18.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
